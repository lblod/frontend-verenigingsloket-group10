<div class="c-account-dropdown" ...attributes>
  <AuButton
    {{this.reference}}
    @icon="user-check"
    aria-haspopup="true"
    aria-expanded="{{if this.dropdownOpen 'true' 'false'}}"
    class="c-account-dropdown__button"
    type="button"
    {{on "click" this.toggleDropdown}}
    data-test-dropdown-button
    data-au-dropdown-toggle
  >
    Omloop het Nieuwsblad
  </AuButton>
  {{#if this.dropdownOpen}}
    <div
      id="account"
      role="menu"
      {{focus-trap
        shouldSelfFocus=true
        focusTrapOptions=(hash
          clickOutsideDeactivates=this.clickOutsideDeactivates
        )
      }}
      {{on "click" this.closeDropdown}}
      class="c-account-dropdown__dialog"
    >
      <div class="c-account-dropdown__header">
        <AuHeading @level="2" @skin="5">Verenigingsloket</AuHeading>
        <AuHelpText @skin="secondary" class="au-u-margin-top-none">Ingelogd als Omloop het Nieuwsblad</AuHelpText>
      </div>
      <AuContent class="c-account-dropdown__content">
        <AuList @divider={{true}} as |item|>
          {{!-- <item>
            <AuLink @route="settings" @icon="settings" role="menuitem">
              Instellingen
            </AuLink>
          </item> --}}
          <item>
            <AuLink @route="login" role="menuitem">
              <AuIcon @icon="exit" />
              Afmelden
            </AuLink>
          </item>
        </AuList>
      </AuContent>
    </div>
  {{/if}}
</div>